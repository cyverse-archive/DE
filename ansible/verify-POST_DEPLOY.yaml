---
# Runs all verification plays


- include: verify-PRE_DEPLOY.yaml
- include: verify-systemd-services-running.yaml
- include: verify-systemd-services-enabled.yaml
- include: verify-executable-jar.yaml

# TODO: Do we need a verify-users-and-groups.yaml for ensuring that we have all the users we need?
